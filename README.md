### Jupiter notebook


review the training script which includes:
1. data loading either from label studio or from roboflow datasets
2. splitting train test samples
3. creating .yaml config file for yolo to get the training instructions
4. run yolo detect to run the training script along with the model name, epochs and image res
5. test the validation set
6. view metrics
7. download optimal model locally

### Python detect inference file
1. run the inference file after setting up the conda env (mentioned in the notebook at the end)
2. either device, file(image/video) or some external viewing source can be tested 
